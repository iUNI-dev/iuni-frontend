import React from 'react';
import { Text, View } from 'react-native';

const VerifyCode = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Pantalla Verify Code (en construcción)</Text>
    </View>
  );
};

export default VerifyCode;
